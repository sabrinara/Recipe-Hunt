/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["hips.hearstapps.com","img.freepik.com","i.ibb.co.com"], // Add the hostname here
      },
};

export default nextConfig;
