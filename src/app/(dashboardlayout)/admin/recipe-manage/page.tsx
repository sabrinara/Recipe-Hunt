import React from 'react';
import RecipeManger from './RecipeManager';

const RecipeManager = () => {
    return (
        <div>
         <RecipeManger/>
        </div>
    );
};

export default RecipeManager;